<nav class="navbar navbar-expand-lg navbar-lewagon fixed-top">
  <div class="container-fluid">
    <!-- Logo e Nome da App -->
    <a class="navbar-brand d-flex align-items-center" href="/">
      <i class="fas fa-music me-2"></i>
      TOCAÍ
    </a>

    <!-- Botão de Toggle para Mobile -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Itens da Navbar (alinhados à direita) -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to 'Meu Perfil', user_profile_path(current_user), class: 'nav-link btn btn-primary me-2' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sair', destroy_user_session_path, data: { turbo_method: :delete }, class: 'nav-link btn btn-secondary' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to 'Entrar', new_user_session_path, class: 'nav-link btn btn-primary me-2' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Cadastrar', new_user_registration_path, class: 'nav-link btn btn-secondary' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
