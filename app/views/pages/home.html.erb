<div class="container mt-5">
  <h1>Bem-vindo ao TOCAÍ</h1>
  <p>Encontre ou cadastre instrumentos musicais para venda!</p>

  <% if user_signed_in? %>
    <%= link_to 'Meu Perfil', user_profile_path(current_user), class: 'btn btn-primary' %>
    <%= link_to 'Sair', destroy_user_session_path, method: :delete, class: 'btn btn-secondary' %>
  <% else %>
    <%= link_to 'Entrar', new_user_session_path, class: 'btn btn-primary' %>
    <%= link_to 'Cadastrar', new_user_registration_path, class: 'btn btn-secondary' %>
  <% end %>
</div>
<h1>Welcome to TOCA!</h1>
<p>You are successfully logged in as <%= current_user.email %></p>
<%= link_to "Sign out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "btn btn-outline-danger" %>
