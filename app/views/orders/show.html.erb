<div class="container">
  <div class="row py-4">
    <div class="col-md-3"><%#= image_tag @order.teddy.photo_url, width: '100%' %></div>
    <div class="col">
      <h1>Pedido #<%= @order.id %> <small><span class="badge badge-primary"><%#= @order.state %></span></small></h1>
      <ul class="list-unstyled">
        <li><strong>EMITIDO EM</strong> <%= @order.created_at.to_date %></li>
        <li><strong>INSTRUMENTO</strong> <%= @order.instrument.name %> [<%= @order.instrument.brand %>]</li>
        <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.amount %></li>
        </li>
      </ul>
      <p>Entrega estimada em <strong>2 dias úteis</strong></p>
      <%= link_to 'Voltar para Home', root_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
